let course = ['hld', 'lld', 'dsa', 6, null,105];
// in other languages type must be same and length but in javascript LENGTH AND TYPE can be  DYNAMIC
// Arrays and functions are object types
console.log(course);
console.log(course[0]);
console.log(course[1]);
console.log(course[5]);
console.log(typeof(course));